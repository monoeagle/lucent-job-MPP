# Session Handoff — 2026-06-19 00:29

## Was wir gemacht haben
Erst die offenen Quelländerungen der Vorsession committet, dann den App-AppImage-AppRun auf
Random-Ports + isolierte Chromium-Instanz gebracht, drei optionale Doku/Tooling-Punkte erledigt
(Insights/Handoffs in Docs-Nav, `run_mpp_docs.sh` PEP-668-gehärtet, Pattern §H), eine vollständige
**VM-Installationsanleitung** (Flask/React, Produktions-Ziel) geschrieben und schließlich vier
**Produktionsreife-Fixes** per TDD umgesetzt.

## Erledigt
- [x] **Commit Vorsession-Arbeit** (`0ed38136`): App-Look-Doku + TDD-Gate + Ein-Befehl-Release (28 Dateien).
- [x] **App-AppRun Random-Ports** (`e70b6f76`): Frontend+Backend ephemeral, gekoppelt an SPA-Proxy,
      isolierte Chromium-Instanz; end-to-end gegen die gebaute AppImage verifiziert (FE/BE-Ports random,
      `/` 200, `/api/v1/health` 200 über Proxy, Cleanup sauber).
- [x] **Insights/Handoffs in Docs-Nav** (`5217bfc5`): `build_docs.py` `step_mirror_insights()` spiegelt
      `docs/insights` + `docs/handoffs` → `mpp-docs/docs/erkenntnisse/` (git-ignored), Nav-Eintrag,
      TDD-Regel `R-ERKENNTNISSE` (fing Pretty-URL-Pfad-Bug, rot→grün). `run_mpp_docs.sh` gehärtet (venv-Python).
- [x] **VM-Install-Guide** (`117c5883`): `docs/deployment/vm-installation.md`, 19 Abschnitte, nginx+gunicorn+
      systemd+PostgreSQL, ehrliche „⚠ Lücke"-Boxen (kein Redis/Celery).
- [x] **Produktionsreife-Fixes** (`9168f693`, TDD): ProxyFix in `create_app`, Readiness-Endpoint
      `GET /api/v1/ready` (DB-`SELECT 1`, 503 bei Ausfall), `migrations/env.py` liest `DATABASE_URL`
      (`app/core/db_url.py`), `gunicorn==23.0.0` in `requirements.txt`. **9 neue Tests grün**,
      gunicorn-Smoke-Test grün, alembic-Override funktional bewiesen.
- [x] **Pattern §H.3** (separates `.pattern`-Repo): App-AppImage mit Backend — beide Ports ephemeral.
- [x] **Doku nachgezogen** (dieser Handoff): Changelog-Session-Block, Test-Zahlen 862→869, Insight, KPI-Zeile.

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (empfohlen):** Die **23 pre-existing Backend-Test-Failures** triagieren — voller Lauf
      `venv/bin/python -m pytest tests/unit tests/integration -q`; betroffen u. a.
      `test_auth_stub_users.py` (`assert 5 == 4` → Stub-User-Count, `test-superadmin` fehlt im Test),
      `test_audit_log_api.py`, `test_dsgvo_api.py`, `test_approval_rules_api.py`. Per `git stash` als
      pre-existing bestätigt (nicht durch diese Session verursacht).
- [ ] **Prod-Blocker:** LDAP/OIDC-Auth implementieren (`app/services/auth_service.py:61` →
      `NotImplementedError`). Ohne das kein Login bei `ENV=production`. Braucht Brainstorming/Spec.
- [ ] Optional: `CMDB_MODE=live`-Client, User-Verwaltung/-Tabelle, HSTS in nginx, `DSGVO_ANONYMIZE`-Flag verdrahten.

## Entscheidungen dieser Session
- **VM-Guide = Zielbild + ehrliche Lücken-Boxen** (nicht „produktionssicher" behaupten), da die App
  echte Produktions-Lücken hat (kein gunicorn/Prod-Auth/CMDB-live/User-Mgmt).
- **Backend ist intern → Backend-Port IMMER ephemeral** (App-AppRun), nur Frontend-Port im Hub fix.
- **`/health` unverändert (Liveness)**, DB-Check als **separater** `/ready` → keine Regression.
- **`gunicorn==23.0.0`** als Prod-Dependency; Start als Factory `app:create_app()`.
- **23 rote Tests bleiben unangetastet** (out of scope „meine Verbesserungen"), aber dokumentiert.

## Git-Status
- Branch: `main`
- Uncommitted: dieser Handoff-Commit (Insight + Handoff + KPI + Changelog/Test-Zahlen + Site-Build)
- Stash: keine
- Commits dieser Session: `0ed38136`, `e70b6f76`, `5217bfc5`, `117c5883`, `9168f693` (+ Handoff-Commit)

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP (Marketplace Portal, Flask + React).
Letzte Session: 2026-06-19 00:29
Kontext: VM-Install-Guide + vier Produktionsreife-Fixes (gunicorn, ProxyFix, Readiness-Endpoint, alembic-DATABASE_URL) per TDD; davor App-AppRun Random-Ports + Docs-Nav-Mirror.
Zuletzt erledigt: `9168f693` Produktionsreife-Fixes (9 Tests grün, gunicorn-Smoke-Test grün), VM-Guide synchronisiert.
Nächster Schritt: Die 23 pre-existing Backend-Test-Failures triagieren (`venv/bin/python -m pytest tests/unit tests/integration -q`; Start: `test_auth_stub_users.py` assert 5==4 / Stub-User-Count). Größerer Brocken danach: LDAP-Auth (`auth_service.py:61`).
Handoff-Datei: docs/handoffs/2026-06-19-0029.md
