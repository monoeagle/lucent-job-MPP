# Session Handoff — 2026-06-27 16:44

> Maßgeblicher End-Handoff der Session 3. Ergänzt [2026-06-27-1532.md](2026-06-27-1532.md)
> (Delivery-/Doku-Parität) um alles, was danach kam: echte Screenshots, Roadmap,
> Galerie-Fix, Repo public + GitHub-Pages-Deploy.

## Was wir gemacht haben
Das Flask/React-MPP an den v1.1.0-Stand des Django-Schwesterprojekts angeglichen
(Delivery + Doku-Site), dann die Oberflächen-Galerie mit echten Playwright-Screenshots
gefüllt, die Roadmap nachgezogen und die Doku öffentlich auf GitHub Pages deployed.

## Erledigt
- [x] **Delivery (D1–D5)**: `run.sh release` + `tools/build_release.py` (Offline-ZIP,
      prebuilt SPA + Wheels), `deploy/install.sh` (AlmaLinux 9: gunicorn-Factory, alembic,
      nginx-SPA + TLS, systemd; kein Celery/Redis), `vm-installation-offline.md`.
- [x] **Doku-Site (K1–K7)**: Oberflächen-Galerie, gh-pages-Skript, Architektur-Badge
      (`index-1.svg`), Heatmap-Klick-Toggle, `.adb-shot-compare`-CSS, v1.1.0-Bump.
- [x] **Echte Screenshots** via Playwright (`scripts/screenshot_tool.py`, superadmin+requester)
      → 12 Galerie-Bilder. Bestellformular-Vergleich korrigiert: **links leer, rechts
      ausgefüllt** (gleicher Linux-Service; leeres Formular per Inline-Playwright ohne Fill-Action).
- [x] **Roadmap aktualisiert**: Gantt (App-Look→done, Sektionen Delivery/Doku-Parität v1.1.0),
      „Offline-Demo-Script" aus Offen entfernt; `todos.md` + `arbeitspakete.md` nachgezogen.
- [x] **Heatmap/Insights**: nach Commit + `build_docs.py` zeigt der 27.06. Aktivität
      (`project-activity.json` byDay enthält 2026-06-27).
- [x] **TDD-Gate grün**, Docs-AppImage v1.1.0 gebaut + global gespiegelt.
- [x] **Repo public gemacht** (`monoeagle/lucent-job-MPP`) + **GitHub Pages aktiviert**
      (gh-pages /, legacy — wie Schwesterprojekt) → **live: https://monoeagle.github.io/lucent-job-MPP/**
- [x] **Pattern-Nachzug (`docs-release-sync.pattern` §A):** Root-`CHANGELOG.md` + `mpp-docs/docs/entwicklung/changelog.md` v1.1.0-Eintrag; `testing.md` + `icon-rail.js`-Badge auf echten Stand (877 = 771 BE + 106 FE).
- [x] **Voller Backend-Test-Lauf** (`venv/bin/python -m pytest tests`, inkl. e2e): **739 grün / 32 rot / 771** — pre-existing (kein App-/Test-Code geändert → keine Regression).
- [x] **Commits**: `32747e12` (Parität v1.1.0), `20ad905a` (Galerie-Formular-Fix), + Doku-Sync-Commit (dieser Handoff).

## In Arbeit / Nächste Schritte
- [ ] **SOFORT (Entscheidung):** Repo ist jetzt **öffentlich** (gesamter Code + Historie sichtbar).
      Falls nicht gewollt → wieder privat (`gh repo edit monoeagle/lucent-job-MPP --visibility private`),
      dann ist aber GitHub Pages auf dem Free-Plan tot.
- [ ] **Auth-Blocker** (unverändert): LDAP/OIDC implementieren — `app/services/auth_service.py:61`
      (`NotImplementedError`). Ohne das kein Login bei `ENV=production`.
- [ ] **32 pre-existing Backend-Failures** triagieren (739✓/32✗/771; approval_rules 9, audit_log 6, dsgvo 4, approval_submit 2, auth_* u. a. — siehe [[test-baseline-and-prod-gaps]]).
- [ ] **`run.sh release` real durchspielen** (lädt Wheels, baut ZIP) + `deploy/install.sh`
      auf echter AlmaLinux-9-VM verifizieren (bisher nur bash -n + Smoke).
- [ ] **`run.sh serve` venv-Bug**: ruft `flask` direkt auf → „Befehl nicht gefunden".
      Fix: Zeile 141 auf `python -m flask run` umstellen (venv-Entry-Points kaputt).
- [ ] Optional: `verify_docs.sh` um R-OBERFLAECHE (Galerie-Vollständigkeit) ergänzen.

## Entscheidungen dieser Session
- **Adaption statt Copy bei Delivery** (Insight `2026-06-27-…adaption-statt-copy`):
  Django-Installer/Doc sind Stil-Zielbild, kein kopierbares Artefakt.
- **SPA prebuilt im Bundle** → Ziel-VM braucht kein Node.
- **Auth-Realität ehrlich**: Installer setzt `AUTH_MODE=ldap` + warnt laut.
- **Galerie an reale UI angepasst** (kein Subscriptions/Profil-Screen erfunden).
- **Repo public für Pages**: Free-Plan unterstützt Pages nur für öffentliche Repos
  (privat → HTTP 422). Bewusst mit User-Freigabe public gemacht.

## Git-Status
- Branch: `main`  ·  Repo: `monoeagle/lucent-job-MPP` (PUBLIC, umbenannt — Origin auf
  kanonische URL gesetzt), `gh-pages`-Branch deployed.
- Uncommitted: nur `.claude/settings.local.json` + `screenshots/v1.0.0/manifest.json`
  (bewusst ausgelassen — User-/Runtime-State).
- Stash: keine.
- Laufende Hintergrund-Server (optional stoppen): Portal :3000 (Vite) + :5000 (Flask via
  `python -m flask`), Docs :5083 (`http.server` über `mpp-docs/site`).

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP (Flask/React Marketplace Portal).
Letzte Session: 2026-06-27 16:44 (Session 3 — Delivery-/Doku-Parität v1.1.0, jetzt public auf GitHub Pages).
Kontext: Portal an den Django-Schwesterstand angeglichen; Doku live unter https://monoeagle.github.io/lucent-job-MPP/.
Zuletzt erledigt: Offline-Release-Workflow + Prod-Installer, Doku-Galerie mit echten Screenshots, Roadmap, Repo public + Pages-Deploy (Commits 32747e12, 20ad905a).
Nächster Schritt (wählen): (a) Auth-Blocker LDAP implementieren (app/services/auth_service.py:61); ODER (b) 23 pre-existing Backend-Failures triagieren (venv/bin/python -m pytest); ODER (c) run.sh release real durchspielen + install.sh auf VM testen.
Handoff-Datei: docs/handoffs/2026-06-27-1644.md
