# Session Handoff — 2026-06-18 23:10

## Was wir gemacht haben
Die MPP-Dokumentation (`mpp-docs/`, Zensical) wurde vom alten Stand auf den aktuellen
`lucent-docs.pattern`-Stand (App-Look §17) gebracht und der Doku-Release-Workflow als
**erzwungenes TDD-Gate** verdrahtet. Kein App-Code geändert — reine Doku-/Build-Tooling-Arbeit.

## Erledigt
- [x] **App-Look angeglichen:** JS-Suite (7 Dateien inkl. lokalem mermaid.min.js, kein CDN),
      `tools/` (Mermaid- + Activity-Pipeline), `build_docs.py` Pipeline-Version, `extra.css`
      Vollversion, `icon-rail.js` adaptiert (Version/Stack/Status-Badge, ADAPT-Reste entfernt).
- [x] **zensical.toml:** `extra_javascript` ergänzt, `navigation.tabs`/`.sticky` entfernt
      (Icon-Rail), `site_description` mit `· v1.0.0` (Hub-Versionsextraktion).
- [x] **Startseite = nur Home-Layout:** Hero = AP-Übersicht (`entwicklung-arbeitspakete-1.svg`)
      + Heatmap + Insights; Fließtext nach `grundlagen/ueberblick.md` ausgelagert.
- [x] **Arbeitspakete + Roadmap:** `entwicklung/arbeitspakete.md` mit AP-Flowchart (`-1.svg`)
      + Gantt (`-2.svg`, retrospektiv aus Git rekonstruiert). Gantt-Button im Header aktiv.
- [x] **Docs-AppImage** gebaut, an **3 Stellen** frisch + byte-gleich: `build/`, `release/`,
      globaler `_Projects/AppImages/`.
- [x] **TDD-Gate** (`mpp-docs/verify_docs.sh`, 11 Regeln) — fing real einen Fehler (ADAPT-Reste),
      rot→fix→grün. In `run.sh docs-appimage` verdrahtet (bricht bei Rot mit Exit 1 ab).
- [x] **`run.sh docs-appimage` = Ein-Befehl-Release:** Stufe-1 Site-Build → AppImage → release/
      → global spiegeln → TDD-Gate.
- [x] **AppRun überarbeitet:** Standalone öffnet **isolierte Chromium-Instanz** (eigenes
      Temp-Profil, `--app`) auf **zufällig freiem Port**; Hub-`--port=` bleibt fix.
- [x] **Patterns zurückgeführt** (in `<projects-root>/.pattern/`, separates Repo):
      `docs-release-sync.pattern` §B.3 (globaler Spiegel), §C (cmp-Check), §G (TDD-Gate, 11 Regeln),
      §H (Standalone-AppImage: Random-Port + isolierte Chromium-Instanz).

## In Arbeit / Nächste Schritte
- [ ] **SOFORT:** Quelländerungen committen (15 Pfade, s. Git-Status) — z. B.
      `git add -A && git commit -m "docs: App-Look + TDD-Gate + Ein-Befehl-Release"`.
      `site/`, `build/`, `release/`, `.venv-docs/`, `.cache/` sind git-ignored (nicht committen).
- [ ] Optional: Random-Port + isolierte-Chromium-AppRun auch in die **App-AppImage**
      (`cmd_appimage` in `run.sh`, ~Zeile 157+) übernehmen — nutzt noch altes `xdg-open`.
- [ ] Optional: `run_mpp_docs.sh` Install-Schritt härten (scheitert an `externally-managed-
      environment`; `docs-appimage` umgeht das bereits via venv-Python).
- [ ] Optional: Insight-/Handoff-Artefakte in den publizierten Docs-Nav aufnehmen (analog
      adb-control-docs `docs/insights/`), falls gewünscht.

## Entscheidungen dieser Session
- **Startseite:** ausschließlich Heatmap + Insights + AP-Übersichtsbild (wie libreDrive/snoocount),
  kein H1, kein Fließtext — Beschreibung lebt auf eigener Seite.
- **APs retrospektiv aus Git** rekonstruiert (MPP hat keine formale `todo.md`-AP-Liste):
  Backend B1–B7, Frontend F1–F7, Integration, Packaging (alle done); 2 offene APs aus `todos.md`.
- **Doku-Port:** Standalone immer zufällig (ephemeral, `bind(127.0.0.1,0)`); fix nur im Hub-Modus.
- **„Fertig" = TDD-Gate grün** — nicht „Build lief durch". Neue Doku-Features bringen zuerst ihre
  Regel (rot), dann die Umsetzung bis grün.

## Git-Status
- Branch: `main`
- Uncommitted: 7 geändert (`.gitignore`, `run.sh`, `mpp-docs/build_docs.py`, `index.md`,
  `extra.css`, `zensical.toml`, `.claude/settings.local.json`), 8 neu
  (`mpp-docs/verify_docs.sh`, `tools/`, `docs/javascripts/`, `docs/images/`, `docs/_data/`,
  `mermaid-sources/`, `docs/entwicklung/arbeitspakete.md`, `docs/grundlagen/ueberblick.md`)
- **Patterns** (`<projects-root>/.pattern/docs-release-sync.pattern`) liegen außerhalb dieses
  Repos — separat sichern, falls versioniert.
- Stash: keine

---
## Resume-Prompt (kopieren für neue Session)

Weitermachen mit MPP (Marketplace Portal).
Letzte Session: 2026-06-18 23:10
Kontext: MPP-Doku auf lucent-docs-Pattern (App-Look) + TDD-Gate-Release-Workflow gebracht; reine Doku/Tooling-Arbeit, kein App-Code.
Zuletzt erledigt: App-Look-Doku komplett, Docs-AppImage an 3 Stellen frisch, TDD-Gate grün, `run.sh docs-appimage` = Ein-Befehl-Release, AppRun mit Random-Port + isolierter Chromium-Instanz, Erkenntnisse in docs-release-sync.pattern §G/§H zurückgeführt.
Nächster Schritt: Quelländerungen committen (15 Pfade), dann optional Random-Port/Chromium-AppRun in App-AppImage (cmd_appimage in run.sh) übernehmen.
Handoff-Datei: docs/handoffs/2026-06-18-2310.md
