# Session Handoff — 2026-06-27 15:32

## Was wir gemacht haben
Das Flask/React-MPP an den voraus gelaufenen Stand des Schwesterprojekts
`lucent-job-mpp-TDD-Django` (v1.1.0) angeglichen — **Delivery + Doku-Site**.
Zuerst beide Projekte analysiert und die Differenzen mit dem User abgestimmt
(GO: „Alles inkl. Prod-Installer", Galerie als Gerüst). Doku-Site-Features 1:1
portiert, Delivery-Artefakte an den Flask/React-Stack adaptiert (kein Celery/
Redis, prebuilt SPA, gunicorn-Factory, alembic).

## Erledigt
- [x] **D2 — Bundle-Builder** `tools/build_release.py`: Allow-list für Flask/React
      (app/, migrations/, alembic.ini, requirements.txt, stubs/, scripts/seed.py,
      wheels/, deploy/, **frontend/dist → frontend-dist/**), Version aus `lucent-hub.yml`,
      `START-HIER.txt` mit Auth-Blocker-Hinweis. Smoke-getestet (105 Dateien, SPA gebündelt).
- [x] **D5 — Produktions-Installer** `deploy/install.sh` (AlmaLinux 9, adaptiert):
      Service-User `/opt/mpp`, venv+Wheels offline, PostgreSQL, `/etc/mpp/mpp.env`
      (JWT_SECRET+DB-PW generiert), `alembic upgrade head` (+optional seed), systemd
      `mpp-web` (gunicorn `app:create_app()`), nginx (SPA statisch + `/api`-Proxy) +
      self-signed TLS, firewalld/SELinux. **Kein** mpp-celery (existiert nicht). bash -n grün.
- [x] **D1/D4 — `run.sh release`-Target** + `APP_VERSION 1.1.0`: baut SPA (npm),
      lädt Wheels (manylinux/cp312), ruft build_release.py. bash -n grün.
- [x] **Offline-Doc** `docs/deployment/vm-installation-offline.md` (Flask/React, air-gapped,
      16 Abschnitte, prebuilt-SPA-Workflow, kein Redis/Celery).
- [x] **K3 — Architektur-Badge** in `icon-rail.js` (`addArchBadge/openArch/archSvgUrl`)
      + neues `mermaid-sources/index-1.mmd` (graph TB, Flask/React-Clean-Architecture)
      → `index-1.svg` gerendert (37 KB).
- [x] **K4 — Heatmap-Klick-Toggle** in `activity-heatmap.js` (`closeDetail`/`activeKey`,
      erneuter Klick schließt Detailliste).
- [x] **K1 — Oberflächen-Galerie** `referenz/oberflaeche.md` (12 echte Screens) +
      Nav-Eintrag + `.adb-shot-compare`-CSS (K5). **Screenshots per Playwright erzeugt**
      (`scripts/screenshot_tool.py`, superadmin+requester, PNG) und in
      `mpp-docs/docs/images/screenshots/Screenshot_01..12_mpp.png` eingebunden; Galerie an
      die real existierenden Seiten angepasst (kein Subscriptions/Profil-Screen — nicht im UI/Tool).
- [x] **Roadmap aktualisiert**: Gantt (`entwicklung-arbeitspakete-2.mmd`) App-Look→done +
      neue Sektionen „Delivery v1.1.0" & „Doku-Parität v1.1.0" (27.06.); „Offline-Demo-Script"
      aus Offen entfernt (erledigt); `todos.md` + `arbeitspakete.md` (Open-Liste) nachgezogen.
- [x] **K2 — gh-pages-Deploy** `mpp-docs/deploy_ghpages.sh` (worktree → gh-pages). bash -n grün.
- [x] **K6 — Home-Layout** Detail-Pane entsperrt (`display:none` entfernt) + Activity-Daten
      refreshed (27.06., 237 Commits, testCount 934).
- [x] **K7 — v1.1.0-Bump** (lucent-hub.yml, run.sh, icon-rail.js, zensical.toml) +
      dieser Handoff + Insight + KPI-Zeile.
- [x] **.gitignore** um `wheels/` + `build/Lucent-MPP-TDD-*/` erweitert.
- [x] **Doku-Site gebaut** (build_docs.py grün: 23 MD, 3 Mermaid-SVG, 4 Erkenntnisse gespiegelt, site/).

## In Arbeit / Nächste Schritte
- [ ] **Commit nötig für Heatmap/Insights/Roadmap-in-git:** Heatmap + Home-Insights werden
      aus `git log` abgeleitet — diese Session ist uncommitted, daher zeigt der 27.06. noch
      keine Aktivität. Nach `git commit` + `build_docs.py` leuchtet der Tag auf und die Stats
      aktualisieren sich.
- [ ] **Release real bauen + testen:** `./run.sh release` (lädt Wheels, baut ZIP) und
      `deploy/install.sh` auf einer echten AlmaLinux-9-VM verifizieren (bisher nur bash -n + Smoke).
- [ ] **Auth-Blocker** (unverändert): LDAP/OIDC implementieren (`app/services/auth_service.py:61`).
      Ohne das kein Login bei `ENV=production` — der Installer dokumentiert das laut.
- [ ] **23 pre-existing Backend-Failures** triagieren (siehe [[test-baseline-and-prod-gaps]]).
- [ ] Optional: `verify_docs.sh`-TDD-Gate ggf. um R-OBERFLAECHE (Galerie-Vollständigkeit) ergänzen.

## Entscheidungen dieser Session
- **Adaption statt Copy bei Delivery** (Insight): Django-Installer/Doc sind Stil-Zielbild,
  kein kopierbares Artefakt — jede Stack-Abweichung (kein Celery/Redis, prebuilt SPA,
  gunicorn-Factory, alembic) wurde explizit umgesetzt.
- **SPA prebuilt im Bundle** → Ziel-VM braucht kein Node.js (Build auf Dev-/Staging-Host).
- **Auth-Realität ehrlich**: Installer setzt `AUTH_MODE=ldap` (korrekte Prod-Haltung) und
  warnt laut, dass der Login bis zur LDAP-Implementierung scheitert (kein Vortäuschen).
- **Galerie als Gerüst** (User-Wunsch): Struktur + Alt-Texte jetzt, Screenshots später.
