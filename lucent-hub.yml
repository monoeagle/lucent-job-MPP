# MPP Marketplace Portal (Flask/TDD) — Lucent Hub App Registration

name: mpp-TDD
display_name: "MPP TDD (Flask)"
description: "Marketplace Portal — Self-Service IT-Provisioning (Flask + TDD, 862 Tests)"
version: "1.0.0"

port: 3000
backend_port: 5000
docs_port: 5083
healthcheck: /api/v1/health
theme_color: "#2196F3"
category: "portale"

setup:
  command: bash scripts/mpp.sh setup
  label: "Setup (venv + DB + migrate)"

tags:
  - portal
  - flask
  - tdd
